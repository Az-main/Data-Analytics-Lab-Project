# Project A — MVTec LOCO Anomaly Detection
# Minimal runtime deps derived from the notebooks + build scripts.
# A full pinned environment freeze is in 00_project_planning/ (and
# 02_audit_reproducibility/requirements_freeze.txt).

# --- core analysis ---
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
scipy>=1.10
pillow>=10.0
matplotlib>=3.7

# --- deliverable builders (dashboard / report / slides) ---
python-docx>=1.1
python-pptx>=0.6
nbformat>=5.9

# --- DINOv2 feature backend (GPU / Colab run only) ---
torch>=2.1
torchvision>=0.16
transformers>=4.40
huggingface_hub>=0.20
